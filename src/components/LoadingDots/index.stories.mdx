import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import LoadingDots from '.';

<Meta title="Components|LoadingDots" component={LoadingDots} />

# Loading Dots
A visual indicator that a process is underway and that the user should await completion.

### Import
```js
import { LoadingDots } from '@astronomer/spectra';
```

### Usage
<Preview>
  <Story name="Default">
    <LoadingDots />
  </Story>
</Preview>

### Props
<Props of={LoadingDots} />

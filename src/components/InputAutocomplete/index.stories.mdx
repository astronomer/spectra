import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import InputAutocomplete from '.';

<Meta title="Inputs|InputAutocomplete" component={InputAutocomplete} />

# Input Autocomplete

### Import
```js
import { InputAutocomplete } from '@astronomer/spectra';
```

### Usage
<Preview>
  <Story name="Default">
    <InputAutocomplete
      name="example"
      onChange={() => null}
      suggestions={[
        'Airflow',
        'Astronomer',
        'Kubernetes',
        'Cloud Containers',
        'Cluster'
      ]}
    />
  </Story>
</Preview>

### Props
<Props of={InputAutocomplete} />

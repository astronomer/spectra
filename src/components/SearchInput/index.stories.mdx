import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import SearchInput from '.';

<Meta title="Components|SearchInput" component={SearchInput} />

# Search Input

A mostly aesthetic wrapper around the Input component to style search inputs.

### Import
```js
import { SearchInput } from '@astronomer/spectra';
```

### Usage
<Preview>
  <Story name="Default">
    <SearchInput name="example" placeholder="Filter by name or email" />
  </Story>
</Preview>

#### Disabled
<Preview>
  <Story name="Disabled">
    <SearchInput name="example" isDisabled placeholder="Filter by name or email" />
  </Story>
</Preview>

### Props
<Props of={SearchInput} />

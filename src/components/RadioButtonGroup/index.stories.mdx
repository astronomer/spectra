import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import RadioButtonGroup from '.';

<Meta title="Inputs|RadioButtonGroup" component={RadioButtonGroup} />

# Radio Button Group
# <small>`import { RadioButtonGroup } from '@astronomer/spectra'`</small>

<Preview>
  <Story name="Default">
    <RadioButtonGroup
      name="example"
      onChange={() => null}
      options={[
        { value: 'foo', label: 'Foo' },
        { value: 'bar', label: 'Bar' }
      ]}
      active="foo"
    />
  </Story>
</Preview>

<Props of={RadioButtonGroup} />
